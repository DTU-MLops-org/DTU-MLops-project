program: train.py
project: mlops-group-42
entity: playing-card-classifier
method: bayes
metric:
  goal: minimize
  name: validation_loss
parameters:
  lr:
    max: 0.1
    min: 0.0001
    distribution: log_uniform
  batch_size:
    values:
    - 16
    - 32
    - 64
  epochs:
    values:
    - 5
    - 10
    - 15
  seed:
    values:
    - 42
run_cap: 10
